<!DOCTYPE html>
<html>
<head>
  <title>Rideshare</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="facebook">
	<% if current_user %>
  	Welcome, <%= current_user.name %>! <br>
  	<%= link_to image_tag( 'facebook-logout-button.png'), signout_path %>
	<% else %>
  	<%= link_to image_tag( 'facebook-login-button.png'), "/auth/facebook" %>
	<% end %>
</div>

<div class="bigbox">
  <div id="logo" align = "center">
    <%= image_tag "logo2.png" %>
  </div>
  
<div class="leftNav">
  <%= link_to 'Search', root_path %>
</div>

<div class="rightNav">
  <%= link_to 'Post/Request a Ride', new_trip_path %>
</div>
  
  <%= yield %>
</div>

</body>
</html>
